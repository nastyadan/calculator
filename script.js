function add(){
let a = Number(prompt('введите число'));
let b = Number(prompt('введите число'));

alert(a + b);
}

function subtract(){
    let a = Number(prompt('введите число'));
    let b = Number(prompt('введите число'));
    
    alert(a - b);
    }

function multiply(){
    let a = Number(prompt('введите число'));
    let b = Number(prompt('введите число'));
        
    alert(a * b);
}

function divide(){
    let a = Number(prompt('введите число'));
    let b = Number(prompt('введите число'));
            
    alert(a / b);
}
